[project]
name = "snippets"
version = "0.1.0"
description = "Algorithm snippet tests"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["patterns/graphs/__tests__"]
pythonpath = ["patterns/graphs"]
